import { IsActiveRouteInListPipe } from './is-active-route-in-list.pipe';

describe('IsActiveRouteInListPipe', () => {
  it('create an instance', () => {
    const pipe = new IsActiveRouteInListPipe();
    expect(pipe).toBeTruthy();
  });
});
