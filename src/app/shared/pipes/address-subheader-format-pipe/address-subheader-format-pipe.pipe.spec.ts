import { AddressSubHeaderFormatPipe } from './address-subheader-format-pipe.pipe';

describe('AddressSubHeaderFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddressSubHeaderFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
