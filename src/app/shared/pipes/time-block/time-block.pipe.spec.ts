import { TimeBlockPipe } from './time-block.pipe';

describe('TimeBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
