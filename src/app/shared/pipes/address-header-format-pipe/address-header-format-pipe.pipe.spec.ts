import { AddressHeaderFormatPipe } from './address-header-format-pipe.pipe';

describe('AddressHeaderFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddressHeaderFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
