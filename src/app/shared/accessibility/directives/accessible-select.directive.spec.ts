import { AccessibleSelectDirective } from './accessible-select.directive';

describe('AccessibleSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessibleSelectDirective();
    expect(directive).toBeTruthy();
  });
});
