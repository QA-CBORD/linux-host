import { EmptyFormControlDirective } from './empty-form-control.directive';

describe('EmptyFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
