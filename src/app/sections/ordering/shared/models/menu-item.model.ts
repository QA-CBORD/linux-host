export interface MenuItem {
  active: boolean;
  deleted: boolean;
  id: string;
  menuItemOptions: any[];
  merchantId: string;
  name: string;
  price: number;
  reportingCategory: string;
  visible: boolean;
}
