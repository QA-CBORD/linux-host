import { DestinationAccountDisplayPipe } from './destination-account-display.pipe';

describe('DestinationAccountDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationAccountDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
