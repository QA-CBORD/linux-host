import { AmountLabelControlPipe } from './amount-label-control.pipe';

describe('AmountLabelControlPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountLabelControlPipe();
    expect(pipe).toBeTruthy();
  });
});
