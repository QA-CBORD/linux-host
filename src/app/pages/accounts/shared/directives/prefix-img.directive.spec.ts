import { PrefixImgDirective } from './prefix-img.directive';

describe('PrefixImgDirective', () => {
  it('should create an instance', () => {
    const directive = new PrefixImgDirective();
    expect(directive).toBeTruthy();
  });
});
