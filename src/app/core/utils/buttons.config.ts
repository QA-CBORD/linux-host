const CANCEL = {
  label: 'CANCEL',
  class: 'clear',
  shape: 'round',
  strong: true,
  fill: 'clear',
};

const RETRY = {
  label: 'RETRY',
  class: 'filled',
  shape: 'round',
  strong: false,
  fill: 'default',
};

const OKAY = {
  label: 'OKAY',
  class: 'filled',
  shape: 'round',
  strong: false,
  fill: 'default',
};

const REDEEM = {
  label: 'REDEEM',
  class: 'filled',
  shape: 'round',
  strong: false,
  fill: 'default',
};

const CLAIM = {
  label: 'CLAIM',
  class: 'filled',
  shape: 'round',
  strong: false,
  fill: 'default',
};

const CLOSE = {
  label: 'CLOSE',
  class: 'clear-with-shadow',
  shape: 'round',
  strong: true,
  fill: 'clear',
};

export const buttons = {
  CANCEL,
  RETRY,
  OKAY,
  REDEEM,
  CLOSE,
  CLAIM,
};
